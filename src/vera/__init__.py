"""VERA: VIS/NIR + 405 nm LIF probe for lunar regolith mineralogy.

This package contains the software pipeline only. There is no hardware code
here — measurements are always read from / written to the canonical CSV schema
defined in :mod:`vera.schema`.
"""

__version__ = "0.1.0"
